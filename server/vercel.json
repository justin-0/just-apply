{
  "version": 2,
  "builds": [
    {
      "src": "server/src/*.ts",
      "use": "@vercel/bun"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/src/$1"
    },
    {
      "src": "/(.*)",
      "dest": "server/src/index.ts"
    }
  ]
}
